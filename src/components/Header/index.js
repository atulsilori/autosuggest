import React from "react";

import "../../styles/header.scss";

const Header = () => {
  return (
    <header className="header-class">
      <span className="icon">Autosuggest</span>
    </header>
  );
};

export default Header;
